# CS-30 Capstone — Personalised AI Learning Assistant (RAG + LLM)

> 这个文件是 Claude Code 每次启动自动读的项目记忆。改动请保持简短，细节放 `docs/`。

## 项目一句话

用 RAG 做一个面向高等教育的**个性化 AI 学习助手**：从可信教学资料里检索证据，再让 LLM 生成有依据、可溯源、能按学生水平调整深浅的解答。

- 项目编号：CS-30 ｜ 客户：Internal（校内导师）
- 交付物：**LaTeX 报告 + 代码**
- 数据集：导师提供
- 领域：Data Science / AI，要求 ML + NLP

## 当前阶段

**第一周框架搭建阶段。** 仓库、CI、接口契约、测试夹具和 mock 流水线已经建立；真实语料解析、切块、向量化、检索、生成与界面仍待实现。

## 项目的两个重心（别偏）

1. **准确性与可靠性** —— 降幻觉、给证据、能拒答。这是项目立项的理由（通用 LLM 会编）。
2. **个性化** —— 同一个问题，对不同基础的学生给不同深度的解释。这一条最容易被做成"加一句 prompt 就算完"，是拉开差距的地方，也最需要早点想清楚怎么评估。

## 方法论约定

- **先有评估集，再谈优化。** 30~50 条问答对是所有实验的前提，没有它后面全是玄学。
- **一次只改一个旋钮**，记录 Recall@k 和正确率的变化。每一次实验 = 消融表里的一行。
- 报告的价值在**对比与归因**，不在系统有多复杂。多模型对比（Llama / Qwen / Gemma / GPT）是官方明确要求的。

## 文档索引

| 文件 | 内容 |
|---|---|
| `docs/项目简介.md` | 官方项目描述与目标，原文摘录，作为范围的最终依据 |
| `docs/RAG优化旋钮清单.md` | 全流水线可调项清单，★ 标记性价比高的。做实验设计时查这个 |
| `docs/构思与待定.md` | 当前的想法、已定/未定的决策、下一步 |
| `docs/系统架构与文献映射.md` | **当前架构基线 v1.3**（冻结候选，自包含）。可单独分发 |
| `docs/System-Architecture-and-Literature-Mapping.md` | 上一份的英文版，供 LaTeX 报告直接取用。两份须同步更新 |
| `docs/archive/学生画像双方案与KG-RAG完整架构_历史版.md` | 历史记录。内容已并入上面两份的 §4，不再单独分发 |

长文档按需读，不要每轮全量加载。

## 协作约定

- 中文交流，代码注释和 commit 用英文。
- 任何影响方向的决策，写进 `docs/构思与待定.md` 的"已定"表格，注明日期和理由。
- 实验结果统一记进消融表，别散落在对话里。
